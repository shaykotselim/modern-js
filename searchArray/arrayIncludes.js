/**
    array.include() method 
    
    The includes() method determine whether an array.includes() a certain value 
    and returns true or false as approprite.

    Ex :- So, in the example above the, if we want to check if 20 is one of the 
    elements in the array, we can do this....

 */
            //Ex :- 01
        const numbersArray = [ 10, 12 , 34 , 43, 22, 2];
        const includesFortyThree = numbersArray.includes(43);
        console.log(includesFortyThree) //Ture;