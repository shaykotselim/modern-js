/**
    * push() method
  
    The push() method is used for adding an element to the end of array;
  
    Note -: It would make sense to add newer items to the end of array, so that 
            we could finish our earlier task finish;

 */

    // Ex - : 1;

        const arr = ['First-item', 'Second-item', 'Third-item',]
        arr.push('Fourth-item');

    console.log(arr.length)// 4
    console.log(arr)//['First-item', 'Second-item', 'Third-item', 'Fourth-item']