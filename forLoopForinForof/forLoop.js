/**
    forLoop() method

    The for loop() is an interative statement which you used to check for certain
    conditions and then repeatdly execute a block of code as long as those 
    conditions are met;


 */
    // Ex -: 1

        for(let i = 0; i < 3; i++){
            let name = 'Selim';
            console.log(name);
        };  // Selim
            // Selim
            // Selim

    // Ex -: 2
        
        for(let i = 0; i < 5 ; i+=2){
            console.log(i)
        };  /// 0
            /// 2
            /// 4
            